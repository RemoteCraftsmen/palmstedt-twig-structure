<div class="parallax {{ class | default('') }}" style="background-image: url('{{ src | default('') }}')">
    <div class="content-wrapper">
        {% include 'components/atoms/heading/heading.html.twig'
            with {
                title: title | default(''),
                level: 2
            }
        %}
        <p>
            {{ description | default('') }}
        </p>
        <div>
            {% include 'components/atoms/button/button.html.twig'
                with {
                    title: cta,
                    class: ctaClass|default('')
                }
            %}
        </div>
    </div>
</div>
